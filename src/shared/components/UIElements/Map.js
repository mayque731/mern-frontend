import React from "react";
import './Map.css'
const Map = props => {
    // const mapRef = useRef();
    // const {center,zoom, mapId} = props;
    // useEffect(() => {
    //     const map = new window.google.maps.Map(mapRef.current,{
    //         center: center,
    //         zoom: zoom,
    //         mapId: mapId
    // }); 
    // new window.google.maps.marker.AdvancedMarkerElement({position: center, map: map, mapId: "mapId"});
    // },[center,zoom]);
    // return <div ref={mapRef} className={`map ${props.className}`} style={props.style}>
    
    // </div>
    
};

export default Map;